# Design — aangeboden-gebruik-api

Retrofit change. Tasks describe retroactive annotation, not new implementation work. The 15 listed methods already exist and implement the 3 REQ(s) below; this change records their observed behavior as a spec and annotates each method with an `@spec` tag pointing at its task.
