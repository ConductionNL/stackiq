# Design — softwarecatalogue-orchestration

Retrofit change. Tasks describe retroactive annotation, not new implementation work. The 33 listed methods already exist and implement the 4 REQ(s) below; this change records their observed behavior as a spec and annotates each method with an `@spec` tag pointing at its task.
