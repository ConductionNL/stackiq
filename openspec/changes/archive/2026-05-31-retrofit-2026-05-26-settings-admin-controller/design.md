# Design — settings-admin-controller

Retrofit change. Tasks describe retroactive annotation, not new implementation work. The 70 listed methods already exist and implement the 5 REQ(s) below; this change records their observed behavior as a spec and annotates each method with an `@spec` tag pointing at its task.
