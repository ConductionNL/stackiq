# Design — contactpersoon-sync

Retrofit change. Tasks describe retroactive annotation, not new implementation work. The 11 listed methods already exist and implement the 2 REQ(s) below; this change records their observed behavior as a spec and annotates each method with an `@spec` tag pointing at its task.
