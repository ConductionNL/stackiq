# Design — dashboard-views-api

Retrofit change. Tasks describe retroactive annotation, not new implementation work. The 9 listed methods already exist and implement the 3 REQ(s) below; this change records their observed behavior as a spec and annotates each method with an `@spec` tag pointing at its task.
